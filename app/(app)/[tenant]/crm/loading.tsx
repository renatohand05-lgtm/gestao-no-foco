import { GFPageSkeleton } from "@/components/gf/gf-skeleton";

/** Loading Signature — CRM / Analytics / Estoque / etc. (Sprint 26.4) */
export default function ModuleLoading() {
  return (
    <div data-page-transition="" data-sprint="26.4" className="p-1">
      <GFPageSkeleton />
    </div>
  );
}
