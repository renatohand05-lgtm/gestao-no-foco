/*
 * Tailwind 4 — imports por caminho explícito de CSS.
 * Evita resolução bare-package que relê o package.json da raiz do projeto
 * (em Windows, leituras transitórias truncadas geravam CssSyntaxError falso
 * em globals.css:1:1 ao parsear JSON incompleto do package.json).
 * Tokens / Design System / motion da Sprint 25.7 permanecem abaixo.
 */
@import "../node_modules/tailwindcss/index.css";
@import "../node_modules/tw-animate-css/dist/tw-animate.css";
@import "../node_modules/shadcn/dist/tailwind.css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-sans);
  --font-mono: var(--font-mono);
  --font-heading: var(--font-display);
  --color-sidebar-ring: var(--sidebar-ring);