# Sprint 30.7 — Triggers

Fonte: `TRIGGER_CATALOG` em `lib/automacoes/triggers.ts`.

Cada gatilho exige `dataSource` canônico e `fields[]` para validação de condições.

Módulos: financeiro, CRM, operação, estoque, compras, metas, inteligência, tributário.

Suite: `npm run test:phase30-automation-triggers`
