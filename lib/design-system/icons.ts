/**
 * Catálogo central de tamanhos e wrappers de ícone do Design System.
 * Use estas constantes em vez de size-* hardcoded.
 */

export {
  dsIconBox,
  dsIconSize,
} from "@/lib/design-system/tokens";

export type DsIconSize = "xs" | "sm" | "md" | "lg" | "xl";
