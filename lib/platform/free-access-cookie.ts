/**
 * Nome do cookie que guarda o código de convite de acesso gratuito
 * (?convite=código) entre o cadastro e a criação da primeira empresa no
 * onboarding — mesmo padrão do cookie de indicação (referral-cookie.ts).
 */
export const FREE_ACCESS_CODE_COOKIE = "gf_free_access_code";
