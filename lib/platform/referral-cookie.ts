/**
 * Nome do cookie que guarda o código de indicação (?ref=código) entre o
 * cadastro e a criação da primeira empresa no onboarding — sobrevive à
 * confirmação de e-mail, que pode acontecer em outra aba/dispositivo.
 */
export const REFERRAL_CODE_COOKIE = "gf_referral_code";
